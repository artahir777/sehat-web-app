import styled from "styled-components";

const CardStyle = styled.div`
    .card{
        
        padding: 0px;
        display: flex;
        align-items: center;

    }
`;

export default CardStyle;