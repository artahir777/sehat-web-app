import styled from "styled-components";

const SectionHeadingStyle = styled.div`
    .horizontal-rule{
        margin: auto;
        text-align: center;
        width: fit-content;
        .heading{
            display: inline-block;
            padding: 10px;
            font-size: 35px;
        }
        img{
            max-width: 50px;
            height: auto;
        }
    }
`

export default SectionHeadingStyle;